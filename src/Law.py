# Ohm's Law

# Calculating Voltage
def voltage(v,i,r):
    return v == i * r

# Calculating Resistance
def resistance (v,i,r):
    return r == v/r

# Calculating Current
def current(v,i,r):
    return i == v/r